services:
  web-terminal:
    image: ghcr.io/robertocjunior/web-dev-server:main
    container_name: web-terminal
    ports:
      - "7681:7681"
    restart: unless-stopped
    # Privileged pode ser necessário para algumas operações de root dentro do container
    privileged: false 
